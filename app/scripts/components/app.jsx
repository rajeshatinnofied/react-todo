(function(React, module, undefined) {
  var Header = require('./Header.jsx');
  module.exports = React.createClass({
    render: function() {
      return (
        <Header/>
      );
    }
  });
}(React, module));